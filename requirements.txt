# Core runtime dependencies (always required)
pydantic>=2.6
pydantic-settings>=2.2
numpy>=1.26
rank-bm25>=0.2.2
python-dotenv>=1.0
tqdm>=4.66

# Embedding + vector store backends (production defaults)
sentence-transformers>=2.6
chromadb>=0.5

# Document loaders
pypdf>=4.2
python-docx>=1.1

# Optional managed embedding providers
# openai>=1.30
# cohere>=5.5

# Notebook / analysis
jupyter>=1.0
matplotlib>=3.8
pandas>=2.2

# Development / quality
pytest>=8.1
pytest-cov>=5.0
ruff>=0.4
black>=24.3
mypy>=1.10
